# Demo UI for Gemma + Neural Memory comparison
FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    gradio>=5.9.0 \
    requests>=2.31.0

COPY app.py .

EXPOSE 7860

CMD ["python", "app.py"]
